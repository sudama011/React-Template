import { calculateAge } from "./dateUtils";
import { setPageTitle } from "./setPageTitle";
import {
  setTheme,
  setLightTheme,
  setDarkTheme,
  setSystemTheme,
  toggleTheme,
} from "./handleTheme";

export {
  calculateAge,
  setPageTitle,
  setTheme,
  setLightTheme,
  setDarkTheme,
  setSystemTheme,
  toggleTheme,
};
